
## built-in TCP input
## $ echo <json> | fluent-cat <tag>
<source>
  type forward
</source>

## built-in UNIX socket input
#<source>
#  type unix
#</source>

# HTTP input
# http://localhost:8888/<tag>?json=<json>
<source>
  type http
  port 8888
</source>

# <source>
#   type serial_input
#   com_port /dev/tty.usbserial-A700eFeP
#   # com_port /dev/tty.usbmodemfa14131
#   baud_rate 9600
#   data dID, sID, value
# </source>

<match serial.**>
  type copy
  <store>
    type mongo
    database serial
    collection data
    buffer_chunk_limit 3k
  </store>
</match>
